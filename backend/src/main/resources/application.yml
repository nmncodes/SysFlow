spring:
  application:
    name: sysflow-backend

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:change-me-in-env-this-is-a-dev-only-default-secret-value}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}

gemini:
  api-key: ${GEMINI_API_KEY:}

# Datasource config intentionally omitted — re-add alongside spring-boot-starter-data-jpa
# in Phase 6 (see docs/05-ROADMAP.md and pom.xml comment).
