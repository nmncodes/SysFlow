spring:
  datasource:
    url: jdbc:h2:mem:sysflow-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

jwt:
  secret: test-only-secret-not-used-in-production-0123456789
  expiration-ms: 3600000
